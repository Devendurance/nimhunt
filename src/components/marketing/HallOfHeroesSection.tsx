import { heroCategories } from '../../data/marketing'
import type { LeaderboardPreview } from '../../data/marketing.types'
export function HallOfHeroesSection({ previewRows }: { previewRows: readonly LeaderboardPreview[] }) {
  return <section className="section heroes-section" id="heroes" aria-labelledby="heroes-heading"><div className="section-heading"><div><span className="section-label">05 / Hall of Heroes</span><h2 id="heroes-heading">The ruins remember.</h2></div><p>Track points, streaks, chests, active expedition time, and NIM collected. Each month, the leaders become Monthly Heroes.</p></div><div className="heroes-layout"><div className="leaderboard"><div className="board-heading"><h3>Most Points</h3><span>Demo rankings</span></div><ol>{previewRows.map(row => <li key={row.rank}><span className="rank">0{row.rank}</span><span>{row.heroName}</span><strong>{row.value}</strong></li>)}</ol><p>No heroes crowned yet.<br />The next name could be yours.</p></div><div className="honours"><span className="section-label">Six ways to be remembered</span><ul>{heroCategories.map(hero => <li key={hero.title}><h3>{hero.title}</h3><span>{hero.category}</span></li>)}</ul></div></div><p className="footnote">Public Hero Names are opt-in. Wallet addresses are not displayed by default.</p></section>
}
