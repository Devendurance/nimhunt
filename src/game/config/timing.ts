/** Authoritative player tile-move tween duration from the gameplay client. */
export const PLAYER_MOVE_DURATION_MS = 160

/** One vsync of timer slack so a real client completing a frame early is not blocked. */
export const PLAYER_MOVE_FRAME_SLACK_MS = 16
